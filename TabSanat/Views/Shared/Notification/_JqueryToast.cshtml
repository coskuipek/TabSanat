<script src="~/assets/libs/jquery-toast/jquery.toast.min.js"></script>
<script>
    var smsg = '@TempData["SMessage"]';
    var emsg = '@TempData["EMessage"]';

    !function (p) {

    "use strict";
    var t = function () { };
    t.prototype.send = function (t, i, o, e, n, a, s, r) {
        a || (a = 3e3), s || (s = 1);
        var c = {
            heading: t,
            text: i,
            position: o,
            loaderBg: e,
            icon: n,
            hideAfter: a,
            stack: s
        };
        r && (c.showHideTransition = r),
            console.log(c),
            p.toast().reset("all"),
            p.toast(c)
    },
        p.NotificationApp = new t,
        p.NotificationApp.Constructor = t
}(window.jQuery), function (i) {
        "use strict";
            if (smsg) {
                i.NotificationApp.send("İşlem Başarılı!", smsg, "top-right", "#5ba035", "success")
            }

            if (emsg) {
                i.NotificationApp.send("Bir hata oldu!", emsg, "top-right", "#bf441d", "error")
            }
}(window.jQuery);
</script>